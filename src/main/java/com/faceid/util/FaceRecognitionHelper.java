package com.faceid.util;

import java.util.Arrays;

public class FaceRecognitionHelper {

    // Comparação simples de arrays de bytes (placeholder para reconhecimento facial real)
    public static boolean compareFaces(byte[] template1, byte[] template2) {
        return Arrays.equals(template1, template2);
    }
}
