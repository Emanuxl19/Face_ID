spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=faceid_dev;encrypt=false;trustServerCertificate=true
    username: sa
    password: P@ssword123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.SQLServerDialect

springdoc:
  swagger-ui:
    enabled: true